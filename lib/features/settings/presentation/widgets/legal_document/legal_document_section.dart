/// Data model for a section in a legal document (license, terms, etc.)
class LegalDocumentSection {
  final String title;
  final String content;

  const LegalDocumentSection(this.title, this.content);
}
